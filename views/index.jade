extends layout

block content
  h1= title
  p Welcome to #{title}
  h1 test
    h2 test
    h2 #{title}
    h2 #{title}
    h1 #{title}
    h1 #{title}
    h1 #{title}
    h1 #{title}
    h1 #{title}
    h1 #{title}
    h1 #{title}
    h2 #{title}
